AppBundle\Entity\Promotion:
  promo-{1..10}:
        name (unique): '<word()>'
        description: '<sentence()>'
        pdfDoc: pdf.url
        fromDate: '<dateTimeBetween("now")>'
        toDate: '<dateTimeBetween("+1 days", "+1 days")>'
        displayFrom: '<dateTimeBetween("now")>'
        displayTo: '<dateTimeBetween("+1 days", "+1 days")>'
        provider: '@provider-*'