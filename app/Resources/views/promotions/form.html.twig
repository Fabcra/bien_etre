{# formulaire des promotions #}
{{ form_start(promoForm) }}
    <div class="background-white p20 mb30">
            <h3 class="page-title">
               Promotion
            </h3>
            <div class="row">
            <div class="" style="margin-bottom: 10px;">
                <div class="form-group col-sm-6">{{ form_row(promoForm.name, {'label' : 'Nom','attr':{'class':'form-control'}}) }}</div>
                <div class="form-group col-sm-6">{{ form_row(promoForm.description, {'label' : 'Description','attr':{'class':'form-control'}}) }}</div>
                <div class="form-group col-sm-3">{{ form_row(promoForm.fromDate, {'label' : 'Date de début','attr':{'class':'form-control'}}) }}</div>
                <div class="form-group col-sm-3">{{ form_row(promoForm.toDate, {'label' : 'Date de fin','attr':{'class':'form-control'}}) }}</div>
                <div class="form-group col-sm-3">{{ form_row(promoForm.displayFrom, {'label' : 'Affichage de','attr':{'class':'form-control'}}) }}</div>
                <div class="form-group col-sm-3">{{ form_row(promoForm.displayTo, {'label' : 'Affichage jusque','attr':{'class':'form-control'}}) }}</div>
                <button type="submit" class="btn btn-primary btn-xs pull-right">Valider</button>

            </div><!-- /.form-group -->
        </div>
    </div>
{{ form_end(promoForm) }}