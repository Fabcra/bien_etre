{% extends 'default/layout_admin.html.twig' %}

{% block rightside %}


            <div class="page-title">
                <h1>
                    Veuillez choisir un
                    {% if (is_granted('ROLE_MEMBER')) %}
                        avatar
                    {% else %}
                        logo
                    {% endif %}
                </h1>
            </div><!-- /.page-title -->


            {{ form_start(imgForm) }}
            <div class="form-group">

                {{ form_row(imgForm.url) }}

            </div><!-- /.form-group -->


            <button type="submit" class="btn btn-primary pull-right">Insérer</button>
            {{ form_end(imgForm) }}

{% endblock %}