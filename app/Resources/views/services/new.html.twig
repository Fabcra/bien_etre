{% extends 'default/layout_update.html.twig' %}
{% block leftside %}
    {% include 'default/nav_update.html.twig' %}
{% endblock %}
{% block rightside %}
    <div class="page-title">
        <h1>{{ service.name }}</h1>
    </div><!-- /.page-title -->
    {{ form_start(serviceForm) }}
    <div class="col-sm-8 col-lg-12">
        <div class="content">
            <div class="background-white p20 mb30">

                <div class="row">
                    <h3 class="page-title">
                        Nouveau service

                    </h3>
                    <div class="form-group col-sm-12" style="margin-bottom: 10px;">
                        <div class="form-group">{{ form_row(serviceForm.name, {'label' : 'Nom','attr':{'class':'form-control'}}) }}</div>
                        <div class="form-group">{{ form_row(serviceForm.description, {'label' : 'Description','attr':{'class':'form-control'}}) }}</div>
                        <div class="form-group">{{ form_row(serviceForm.image, {'label' : 'Image'}) }}</div>

                        <button type="submit" class="btn btn-primary btn-xs pull-right">Valider</button>
                    </div><!-- /.form-group -->
                </div>
            </div>
        </div>
    </div>
    {{ form_end(serviceForm) }}
{% endblock %}